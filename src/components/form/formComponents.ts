import { FormGroup, FormLegend, FormRoot, FormSubmitButton } from './index';

export const formComponents = {
  Root: FormRoot,
  Group: FormGroup,
  Legend: FormLegend,
  SubmitButton: FormSubmitButton,
} as const